require 'spec_helper'

describe MaterialForm do
  it 'has a version number' do
    expect(MaterialForm::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
